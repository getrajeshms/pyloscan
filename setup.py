from setuptools import setup

# This is not a package - it's a Streamlit application
# No packages to install, just dependencies
setup(
    name="pyloscan",
    version="1.0.0",
    description="PyloScan - AI-powered H. Pylori infection prediction system",
    py_modules=[],
    packages=[],
)
